name: vasili_mentoring_tasks
description: Flutter application for Mentoring program.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  chopper: 3.0.6
  json_serializable: ^3.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.11.0
  chopper_generator: 3.0.6

flutter:
  uses-material-design: true
